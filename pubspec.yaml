name: taller_flutter
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase (versiones REALES que existen)
  firebase_core: ^3.15.2
  firebase_auth: ^5.5.0
  firebase_database: ^11.3.0
  firebase_storage: ^12.3.0
  
  # Google Sign-In
  google_sign_in: ^6.3.0
  
  # Tus dependencias actuales
  youtube_player_flutter: ^8.1.2
  url_launcher: ^6.2.5
  share_plus: ^7.2.2
  cached_network_image: ^3.3.1
  
  # Nuevas
  video_player: ^2.10.0
  file_picker: ^9.2.0
  image_picker: ^1.2.0
  provider: ^6.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/peliculas.json